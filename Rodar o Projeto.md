no terminal do front: npm start
no terminal do back:  entre primeiro na pasta do backend (cd backend) depois rode isso: mvn -q -f backend/pom.xml clean package && java -jar backend/target/backend-1.0.0.jar